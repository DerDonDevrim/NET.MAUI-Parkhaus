<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ParkhausApp"
             x:Class="ParkhausApp.App">
    <Application.Resources>
        <ResourceDictionary>
            <Color x:Key="Primary">#4169e1</Color> 
            <Color x:Key="Background">#87cefa</Color>
            <Color x:Key="TicketColor">#ffffff</Color>
            
            <Color x:Key="Text">#0F172A</Color>
            <Color x:Key="SubText">#64748B</Color>
            <Color x:Key="Border">#E2E8F0</Color>

            <Style TargetType="ContentPage">
                <Setter Property="BackgroundColor" Value="{StaticResource Background}"/>
            </Style>

            <Style x:Key="TitleLabel" TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource Text}"/>
                <Setter Property="FontSize" Value="26"/>
                <Setter Property="FontAttributes" Value="Bold"/>
            </Style>
            
            <Style x:Key="SubTitleLabel" TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource SubText}"/>
                <Setter Property="FontSize" Value="14"/>

            </Style>

            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource Primary}"/>
                <Setter Property="TextColor" Value="White"/>
                <Setter Property="CornerRadius" Value="16"/>
                <Setter Property="HeightRequest" Value="52"/>
                <Setter Property="FontAttributes" Value="Bold"/>
            </Style>
            
            <Style x:Key ="TicketFrame" TargetType="Frame">
            <Setter Property="BackgroundColor" Value="{StaticResource TicketColor}"/>
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="HasShadow" Value="True"/>
            <Setter Property="BorderColor" Value="{StaticResource Border}"/>
            </Style>
                

        </ResourceDictionary>
    </Application.Resources>
</Application>
