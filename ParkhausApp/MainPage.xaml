<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ParkhausApp.MainPage" BackgroundColor="{StaticResource Background}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="14">
            <Label Text="Parkhäuser in der Nähe" Style="{StaticResource TitleLabel}"/>

            <Frame Style="{StaticResource TicketFrame}">
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                <Button x:Name="parking1" Text=""
                    Clicked =  "chose"/>


            </Grid>
            </Frame>
            <Frame Style="{StaticResource TicketFrame}">
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
            <Button x:Name="parking2" Text=""
                    Clicked =  "chose"/>
            </Grid>
            </Frame>
            <Frame Style="{StaticResource TicketFrame}">
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
            <Button x:Name="parking3" Text=""
                    Clicked = "chose"/>
            </Grid>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
